"use client";

/**
 * Re-export from Zustand store for backward compatibility.
 * @deprecated Use useDockVisibilityStore directly from "@/stores/dock-visibility-store"
 */
export { useDockVisibilityStore as useDockVisibility } from "@/stores/dock-visibility-store";
